# DECISIONS

- 2026-09-22: **إصلاح CSP على Workers/Pages ونشر ناجح + تصحيح اسم الـ Worker** — السبب الجذري لانكسار التنسيق على `mahmoudrabeh.mahmoud-rabeh.workers.dev` و`portfolio.pages.dev`: `_headers` كانت تحمل CSP قديم `script-src 'self'` يمنع Tailwind CDN (`cdn.tailwindcss.com` — محرك التنسيق الوحيد، لا css محلي) وكل الـ inline scripts (7 كتل: لغة/FAQ/QR/Form). GitHub Pages يتجاهل `_headers` (لهذا كان سليماً هناك)؛ Vercel بلا CSP أصلاً. **الإصلاح:** `script-src 'self' 'unsafe-inline' https://cdn.tailwindcss.com` + استثناءات `.assetsignore` جديدة (`.git` — كان يرفع كائن LFS 461MB ويكشف `/git/config` علناً! + `bot/` — كان يكشف `bot.py` + `job-hunt-before*.png` و`uiux_demo*.png` و`jpg.png` — 30MB كانت تسبب `UND_ERR_HEADERS_TIMEOUT`). **اكتشاف:** `wrangler.jsonc` كانت `name: portfolio` بينما الرابط الحي `mahmoudrabeh` — النشر الأول ذهب لـ Worker خاطئ؛ التصحيح: `name: mahmoudrabeh` + إعادة نشر → الحي بالـ CSP الجديد Version `884df74a` + فحص أمني: `.git/config` و`bot/bot.py` و`bot/.env` كلها 404. **القواعد:** (1) أي نشر Workers/Pages يتطلب مراجعة `_headers` + `.assetsignore` (الرفع من القرص لا من git — كل مجلد الـ repo أصول عامة). (2) تحقق من تطابق `wrangler.jsonc name` مع رابط workers.dev المعروف للمستخدم قبل النشر. (3) wrangler OAuth يخزّن في `AppData\Roaming\xdg.config\.wrangler` وليس `~/.wrangler`.
- 2026-09-22: **إعداد Cloudflare الرسمي للوكيل (agent-setup)** — نُفّذت تعليمات `https://developers.cloudflare.com/agent-setup/prompt.md` بالكامل: (1) ثُبّتت 14 مهارة Cloudflare عبر `npx -y skills add cloudflare/skills --skill '*' --yes --global` إلى `~\.agents\skills\` (المسار القياسي الذي يكتشفه OpenCode تلقائياً — فشل PromptScript فقط لأنه لا يدعم التثبيت العام ولا يهمنا)، (2) أُضيفت 5 خوادم MCP بعيدة في `opencode.jsonc` تحت `"mcp"`: `cloudflare` (mcp.cloudflare.com/mcp + oauth) + `cloudflare-docs` (عام بلا مصادقة) + `cloudflare-bindings` + `cloudflare-builds` + `cloudflare-observability` (كلها oauth:{}). التحقق: JSONC سليم (بارسر يحترم النصوص) + الأندبوانتات حية (401 = يحتاج OAuth / 405 = حي) + `opencode mcp list` يعرضها كلها (docs متصل فوراً والباقي needs authentication). **القاعدة:** OAuth ينطلق تلقائياً عند أول استخدام لأداة Cloudflare — لا حاجة لتشغيل `opencode mcp auth` يدوياً. يتطلب إعادة تشغيل opencode لتحميل الخوادم والمهارات. **درس التحقق:** إزالة التعليقات عبر regex بسيط (`//.*?$`) تكسر الروابط داخل النصوص (`https://`) — استخدم بارسر يحترم الـ strings لفحص JSONC.
- 2026-09-21: **إضافة موديلات DeepSeek و Kimi للسرب** — أضيف providers جديدة في `C:\Users\RTX\.config\opencode\opencode.jsonc`:
  - `deepseek` (api: https://api.deepseek.com/v1) مع موديلات `deepseek-chat` (64k context) و `deepseek-reasoner` (64k context) — يتطلب `DEEPSEEK_API_KEY`.
  - `kimi` / `moonshot` (api: https://api.moonshot.cn/v1) مع موديلات `kimi-k2` (128k context) و `kimi-k3` (200k context) — يتطلب `MOONSHOT_API_KEY`.
  - كلاهما خارج `disabled_providers` (يعملان عند إضافة المفاتيح). تم التحقق: `opencode run --model deepseek/deepseek-chat` و `kimi/kimi-k2` يتعرفان على الموديلات (خطأ 401 = مفتاح مطلوب، وليس خطأ إعداد).
  - الهدف: توسيع خيارات السحابة للمهام المعقدة (DeepSeek للتفكير/الكود، Kimi للسياق الطويل).

- 2026-09-18: **توسيع بوت المراقب بميزات جديدة (طلبية)** — (1) **تلخيص فيديو**: كشف تلقائي لروابط يوتيوب/فيميو/تيك توك/فيسبوك/انستغرام في `handle_any_text` قبل كل شيء (حتى قبل أوامر المشاريع) → رد فوري «🎬 ⏳» → vidscribe بِـ backend native (faster-whisper محلي بلا Docker) + OpenRouter (inclusionai/ling-3.0-flash-fin:free). **قاعدة كشف الروابط قبل أي منطق آخر** — الروابط كانت تُتجاهل قبل إضافة هذه الخطوة. (2) **بحث متقدم `/search`**: DuckDuckGo Instant Answer API (مجاني بلا مفتاح) — لا حاجة لـ Firecrawl/دفع. (3) **مساعد شخصي `/assist`**: ملف `profile.json` (اسم/مسمى/مهارات/أهداف مهنية ومالية/ملاحظات) + أوامر إضافة وتحديث + أي سؤال آخر يُحوَّل لاستشارة AI بسياق الملف (mimo 60s). **المبدأ:** الخبرة الشخصية مخزنة + الموديل يبني عليها → رد عملي مرقم بدل رد عام.
- 2026-09-18: **وكلاء مساعدون للمهام الطويلة** — بطلب المستخدم أُنشئت 3 ملفات وكلاء في `~/.config/opencode/agent/`: `implementer` (nemotron-3-ultra-free — تنفيذ ثقيل) + `inspector` (ling-3.0-flash-fin-free — فحوصات سريعة) + `planner` (inkling:free — بحث/تخطيط). قاعدة: 2-3 وكلاء متوازين كحد أقصى (429 بمزود مجاني). **درس:** frontmatter الوكيل لا يقبل حقل `tools` (يفشل opencode كلّه) — تُضبط الصلاحيات عبر `permission` فقط.
- 2026-09-18: **إصلاح صمت بوت المراقب** — الرد العام (fallback) كان `opencode run` بمهلة 90 ثانية بلا تأكيد فوري. القرار: رد «⏳ جاري التفكير...» فوراً + مهلة 45 + تسجيل الرسائل الواردة + تسجيل stdout/stderr عند الفشل. **قاعدة:** أي مسار بطيء في البوت يجب أن يرد إشارة انتظار فوراً قبل التنفيذ.
- 2026-09-18: **Docker معطّل حتى تثبيت WSL** — التحقق أثبت: `wsl -l -v` = لا توزيعة مثبتة → محرك Linux يرد 500 دائماً على /version. الحل الوحيد: `wsl --install -d Ubuntu` (مشغّل كمسؤول + إعادة تشغيل). vidscribe يبقى شغالاً عبر whisper المحلي بلا حاوية.
- 2026-09-18: **LinkedIn عبر Composio** — رابط مصادقة جاهز (ينتهي خلال 10 دقائق). أدوات النشر (create_post/get_my_info/company_info/image_upload) تُستخدم فقط بعد اتصال نشط مؤكد. البحث عن وظائف مباشر عبر DICE_MCP + ZIPRECRUITER_MCP (بلا مصادقة).

- 2026-09-17: **تفعيل v4 نسخة نشطة بموافقة المستخدم** — بعد معاينة ثانية على 8771 (0 أخطاء + لقطة + كل العلامات حية في الجذر). نُسخ `portfolio.html` فقط للجذر (باقي الملفات متطابقة بالبصمة) + نسخة احتياطية من `v3` في `versions/v3-2026-09-17-backup/` + فحص أمني سليم (0 توكنات/مفاتيح) + دفع. **القاعدة المحفوظة:** إصلاح الشاشات كان داخل `v4` فتحافظت الجوالات.
- 2026-09-16: **إزالة Oracle/ERP من مهارات مطابقة الوظائف** — بطلب المستخدم حُذفت `oracle/sap/odoo/erp/netsuite` من ملف المُرشح (43 → 38 مهارة) في `job_aggregator.py` + `smart_scorer.py` + KEYWORDS القديمة + وسوم setup.html (استُبدلت بـ Negotiation/Supply Chain/المشتريات). محلل الفجوات والتقارير المُولّدة ما زالت تذكرها (تُحدَّث عند إعادة التوليد).
- 2026-09-16: **شهادة Oracle SCM ملغاة مؤقتاً** — أُزيلت من قوائم الانتظار في portfolio + job-hunt مع وسم التاريخ. تفعيل Stitch أُجّل: بحث رسمي أكد أن GCP يشترط كارت بنكي (تحقق فقط — لا سحب إلا بترقية يدوية) + رصيد 300$ لـ 90 يوم. **التوصية:** إبقاء Stitch معطلاً — احتياجات التصميم مغطاة (14 مهارة + chrome-devtools + uiux-pro).
- 2026-09-16: **معاينة v4 تمت — بانتظار موافقة المستخدم**: خادم محلي 8771 + Playwright (عنوان سليم + 0 أخطاء console + لقطة). **القاعدة:** لا تفعيل (نسخ للجذر) إلا بكلمة "فعّل" صريحة من المستخدم.
- 2026-09-16: **تثبيت Firecrawl MCP + Composio (نسختان سحابيتان)** — بحث ويب أكد: Firecrawl الرسمي `firecrawl/firecrawl-mcp-server` (7,413⭐ — `npx -y firecrawl-mcp` + مفتاح `FIRECRAWL_API_KEY`) + بديل مستضاف بلا مفتاح `https://mcp.firecrawl.dev/v2/mcp` (طبقة مجانية: scrape/search/parse فقط)؛ Composio بوابة واحدة `https://connect.composio.dev/mcp` (ألف تكامل عبر meta-tools + مصادقة `opencode mcp auth composio`). **القرار:** تثبيت النسختين البعيدتين (remote) لا المحليتين — لا عمليات node جديدة ولا إبطاء إقلاع. لا مفتاح `FIRECRAWL_API_KEY` ولا `COMPOSIO_API_KEY` في env (فُحص — غير موجودين). التحقق: JSONC سليم (12 خادماً — 5 مفعّلة) + الوصول حي (Firecrawl 405 + Composio 401 — كلتاهما تستلزمان POST/auth كما هو متوقع). **الخطوة التالية:** إعادة تشغيل opencode + `opencode mcp auth composio` + مفتاح Firecrawl اختياري لاحقاً (crawl/map/agent).
- 2026-09-16: **إصلاح قاتل في الداشبورد**: `dashboard.html` — الـ iframes كانت تشير لنفسها (`src=""` → المتصفح يفسّره كعنوان الصفحة الحالية). **الحل:** إزالة `src=""` بالكامل والاستخدام فقط `data-src` مع تعيين `src` عند النقر (lazy loading). كل 4 التبويبات تعمل الآن ✅.
- 2026-09-16: **API تصفية جديد** `/api/search` في `job_app.py` — يقبل: source, work_type, min_score, q (بحث نصي), skills, location, limit. يُرجع وظائف مع تحليل CV. **إضافة حاسمة:** `dict(j)` لتحويل `sqlite3.Row` → dict (كان يسبب 500 error).
- 2026-09-16: **صفحة فلتر ذكية** `setup.html`: بدل صفحة إعدادات تحوّلت لصفحة فلتر + نتائج مباشرة — حقل بحث نصي + بطاقات مصادر قابلة للتحديد + فلترة حسب المصدر ونوع العمل + عرض النتائج في الوقت الحقيقي (AJAX) + أزرار تصنيف (الكل/عالٍ/متوسط/منخفض) + رابط العودة للداشبورد. **مُختبَر**: 150 وظيفة مطابقة تظهر ✅.
- 2026-09-16: **تحسين سرعة إقلاع opencode — تعطيل 5 خوادم MCP + تنظيف 12 عملية زومبي**: التشخيص كشف 8 خوادم MCP نشطة (≈27s startup) + 33 عملية node عالقة من جلسات متعددة لم تمت. **الحل:** تعطيل context7 + github + mcp-ui + magic-ui + shadcn-ui (5 خوادم) → الإبقاء على playwright + chrome-devtools + uiux-pro (3 فقط). قُتلت 12 عملية زومبي (context7 + github). **القاعدة:** تعطيل خادم MCP في config لا يقتل عملياته العالقة — يجب قتلها يدوياً عبر `Stop-Process`. **الملاحظة:** GITHUB_TOKEN موجود فعلاً في User env (لم يكن معطّلاً لغياب التوكن، بل للسرعة فقط). **التغييرات تحتاج إعادة تشغيل opencode.**
- 2026-09-15: **أدوات التصميم كمرجع موحّد لكل المشاريع** — نتائج بحث GitHub API + Google Stitch: (1) أُنشئ `C:\Users\RTX\.config\opencode\DESIGN-REFERENCES.md` (جدول اختيار الأداة حسب الاحتياج + أفضل 10 MCP + كل المهارات) وأُضيف إلى instructions ليُقرأ كل جلسة، (2) **14 مهارة تصميم منتقاة** انُتخبت بعناية (585KB) من مستودعات كاملة (design-extract = 26MB + taste = 1.7MB كانت ستثقل السياق)، (3) MCP `chrome-devtools` (52K⭐) مُفعّل، (4) MCP `stitch` (Google) معطّل بانتظار إعداد GCP. **القاعدة:** أي أداة تصميم جديدة مستقبلاً تُحمَّل فقط إن كانت في ملف المرجع أو عبر `skills.paths`.
- 2026-09-15: **نسخة v4 من البورتفوليو — تحسينات UI/UX MCP** (بانتظار موافقة المستخدم): أُضيفت في `versions/v4-2026-09-15/` — تطبيق معايير uiux-pro + shadcn + magic-ui: (1) تصميم توكنز نظامي (مقياس 4px + radius + elevation + z-index + motion)، (2) شريط تقدم القراءة أعلى الصفحة، (3) **نظام توست بدل alert** في نموذج التواصل، (4) focus-visible حسب WCAG، (5) احترام prefers-reduced-motion، (6) رفع بطاقات glass عند hover + ضغط أزرار عند النقر. **القاعدة:** لن يُفعَّل حتى يراها المستخدم في المعاينة ويوافق (قاعدة 14-15). إن وافق: انسخ v4 → الجذر + فحص أمني + push.
- 2026-09-15: **أُضيف 5 خوادم UI/UX MCP** إلى الإعداد العام: mcp-ui (5,117 نجمة — واجهات تفاعلية) + figma (يحتاج FIGMA_API_KEY — معطّل حالياً) + magic-ui (مكونات React) + uiux-pro (1,920+ مورد تصميم) + shadcn-ui (مكونات React). **السبب:** المستخدم يريد أدوات تصميم UI/UX متكاملة مع AI. **الملاحظة:** Figma يحتاج توكن شخصي — يُفعّل عند إضافته.
- 2026-09-15: **أدوات النظام/الطرفية انتقلت إلى `D:\ai\tools\` مستقل** — بعد ملاحظة المستخدم المحقة ("لماذا لم تصنع له مجلداً جديداً؟")، كل ملفات الـ Launcher انتقلت من `D:\ai\portfolio\tools\` (ريبو عام منشور) إلى `D:\ai\tools\`. **القاعدة:** ريبو البورتفوليو = كود الموقع فقط؛ أي أداة مساعدة على الجهاز → مجلدها المستقل. البنية الجديدة: `apps\<id>\app.json` لكل أداة + واجهة `/app/<id>` منفصلة + مولّد `make_apps.py`. مسارات أُصلحت في: `start_bots.vbs` + WORK-INDEX/LEARNING.
- 2026-09-13: **فصل المسار التعليمي/التسويقي في مشروع مستقل** — نُقلت ملفات `docs/marketing/*` إلى `D:\ai\media-buying\` (مشروع خاص بذاكرة مستقلة AGENTS/PROGRESS/NEXT_STEPS/DECISIONS). **السبب:** (1) الخصوصية — كانت في الريبو العام winner-dev، الآن خاصة. (2) تعددية المهام — وكلاء متوازون على مشاريع مختلفة بلا تعارض git. ريبو البورتفوليو = كود الموقع فقط.
- 2026-09-13: **اللينكدإن = البروفايل الشخصي** (تأكيد المستخدم) — no company page. دليل النسخ-اللصق في `D:\ai\media-buying\docs\marketing\LINKEDIN-PROFILE-COPY.md`.
- 2026-09-13: **MCP Filesystem ممنوع نهائياً على ويندوز** — الحزم `@modelcontextprotocol/server-filesystem` (npx) و`mcp-filesystem` (uvx) علّقت ويندوز مرتين. الأفضل: الأدوات المدمجة (glob/grep/read/write/edit) تغني عنه. قاعدة عامة: أي MCP جديد يُختبر في جلسة قصيرة وعند أول علامة عطل يُحذف فوراً.
- 2026-09-13: **MCP المثبت في الإعداد العام**: playwright (معاينة بصرية تلقائية) + context7 (وثائق محدثة) + github (يحتاج GITHUB_TOKEN env / لم يُنشأ بعد). GitHub MCP يعتمد على توكن شخصي — ينشئه المستخدم من GitHub Settings → Developer Settings → Tokens.
- 2026-09-13: تحسينات SEO للموقع الحي (winner-dev): canonical/OG/Twitter/JSON-LD/description/favicon + تصحيح robots.txt + sitemap.xml من النطاق الخاطئ (mahmoud-rabeh.com) إلى النطاق الحي. **درس:** أي تحديث مستقبلي للنطاق/النشر — تحقق أولاً من robots.txt + sitemap.xml + canonical (هم الثلاثة المعرضون للنسيان).
- 2026-09-13: حزمة التسويق ملفاتها جاهزة في `docs/marketing/` (منشورات لينكدإن 10 عربي/إنجليزي + خطة سوشيال مصر + خارطة أفلييت Noon/Jumia/Amazon + قائمة إعداد الحسابات). **الخطوة القادمة إجراء المستخدم:** تكوين LinkedIn أولاً ثم النشر. لا أرقام غير مؤكدة + لا أسعار في البروفايلات/المنشورات (قواعد موجودة في الملفات).
- Versioning system: every design version stored as a complete self-contained folder under `versions/vX-YYYY-MM-DD/` + registry in VERSIONS.md. Switching = copy chosen version's files to root. Current active = **v3 (2026-09-13 activated, responsive fixed, user-approved)**; v1 backup at `versions/v1-2026-09-13-backup/`.
- Media buying strategy: learn for self first (small budget 100-150 EGP/day) → prove results → then sell as a service. Scaling rule: max +30-40% budget increase at a time.
- Word reference doc (Media-Buyer-Reference.docx) is the living knowledge base — user must only say "حدّث المرجع" to update it continuously.
- OpenRouter free models: added all models with pricing 0, including openai/gpt-oss models per user confirmation they are free.
- Session continuity: use file-based handover (PROGRESS.md/NEXT_STEPS.md/DECISIONS.md) instead of relying on session resume only.
- Missing handover files: build agent auto-creates PROGRESS.md, NEXT_STEPS.md, DECISIONS.md when absent in a project.
- Created MASTER WORK PROTOCOL (PROTOCOL.md): unified protocol distilled from real patterns across all projects (winner druge / lead-intel / ai studio / pharmacy / portfolio). Level 1 = shared core (12 rules), Level 2 = one-question-at-a-time classification survey, Level 3 = tiered structure (simple/medium/enterprise), Level 4 = reusable project-start prompt.
- Added Mentoring Mode (Level 5) to PROTOCOL.md: tiered explanation system (brief/simple/lead/deep) with stop commands (kafi/sufficient/yalla).
- Created LEARNING.md (global, cumulative): persistent memory across all projects. Updated automatically after every project session. Covers: accumulated skills, winning patterns, lessons learned, user preferences, project registry, and cumulative stats.
- Added LEARNING.md to global opencode.jsonc instructions array — auto-loaded in every session.
- Updated build agent prompt to read AND update LEARNING.md at start/end of every session.
- Added CHANGELOG.md requirement to all tiers in PROTOCOL.md.
- Added COMMON_LANGUAGE.md (multi-model collaboration protocol) to medium tier.
- Added Resilience/Fallback strategy pattern to enterprise tier (from ai studio + pharmacy patterns: local storage, JSON fallback, pipeline error handling).
- Removed Knowledge Center section from portfolio.html (all items were "Coming Soon" with no content — weakens credibility).
- Removed Knowledge Center card from brands-overview section + all nav/footer links to it.
- Moved floating social bar (WhatsApp, Telegram, LinkedIn) from right (RTL inset-inline-start) to left (fixed left:14px) — user requested all social icons on the left.
- Unified language toggle to a single button (removed langToggleMobile from mobile menu). Button text matches current language: "Change Language" in English, "تغيير اللغة" in Arabic.
- Unified language toggle to a single button (removed langToggleMobile from mobile menu). Button text matches current language: "Change Language" in English, "تغيير اللغة" in Arabic.
- 2026-09-12: Preview Rule — بعد كل تعديل مهم (UI/محتوى/تصميم) يفتح المساعد المعاينة في المتصفح فوراً ولا ينتقل للخطوة التالية قبل رؤية المستخدم للنتيجة. محفوظة في AGENTS.md (مشروع) + PROTOCOL.md قاعدة 14-15 (عالمي).
- 2026-09-13: القرار النهائي — **لا تحميل أي نماذج Ollama محلية إضافية**. النماذج المحلية (qwen3:8b, qwen3-fast, qwen2.5-coder:1.5b) لا تصلح للبرمجة مع OpenCode (tool-calling يفشل/يعلّق + بطء prefill على Ryzen 3750H). الاستخدام المحلي المصرح به فقط: `ollama run qwen2.5-coder:1.5b` للأسئلة/الأكواد المباشرة السريعة. بوت المراقب + البرمجة عبر OpenCode = **سحابي فقط** (mimo افتراضياً، OpenRouter/Anthropic/OpenAI بعد إضافة المفاتيح).
- 2026-09-13: تمت إضافة موفري الوكلاء المجانية (duckai, opencodefree, freellmpool, freellmapi, freeinference + نماذج groq/google/openrouter/nim/mistral/sambanova/github-models) في opencode.jsonc العام + سكربت start-proxies.ps1/.bat/.sh في `~/free-ai-proxies`. **النتائج الفعلية بعد الاختبار:** duckai → قطع جغرافي (يحتاج VPN)؛ opencodefree → 429 حد طلبات Zen المجاني (مؤقت، يبرد)؛ freellmpool → سقف ميزانية Pollinations المشتركة (غير قابل للاستخدام حالياً)؛ mimo المدمج (opencode/mimo-v2.5-free) يبقى الخيار المجاني العملي الوحيد بلا حدود.
- 2026-09-14: **أُضيف subagent رابع `researcher`** (إعداد عام) — موديل `openrouter/thinkingmachines/inkling:free` (مُختبَر شغال 2026-09-14) مهمته البحث والمقارنة بين الأدوات عبر الويب. **درس:** نماذج OpenRouter `:free` تُسحب وتُحوَّل لمدفوعة فجأة — اختبر أي موديل جديد بـ `opencode run --model ... "رد بكلمة واحدة"` قبل الاعتماد عليه (minimax-m3 / deepseek-v4-flash / z-ai/glm-5.2 كلها أصبحت مدفوعة). **درس 2:** أداة websearch غير متاحة في `opencode run` غير التفاعلي لكنها متاحة للوكلاء الفرعيين في الجلسة التفاعلية.
- 2026-09-13: **فلترة قائمة الموديلات** عبر `disabled_providers` — أوقف الموفرين بلا مفاتيح (anthropic, openai, google, groq, cerebras, mistral, sambanova, nim, github-models, aiand, freeinference) لتنظيف `/models` من مئات النماذج المدمجة غير القابلة للاستدعاء (576 → 43). **استثناء حاسم:** openrouter أُعيد فوراً لأنه **يعمل بلا مفتاح** (نماذجه `:free` كانت تعمل للمستخدم وكانت افتراضية الجهاز) + مفتاحه مسجل فعلاً في env المستخدم (`OPENROUTER_API_KEY`) **وفي** `%USERPROFILE%\.local\share\opencode\auth.json` (من /connect سابقاً) — أي مفاتيح لم تُحذف مطلقاً (إخفاء القائمة لا يمس البيانات). التحقق النهائي: `opencode run --model opencode/big-pickle` → رد OK، والقائمة 414 بعد إعادة openrouter. **درس:** قبل تعطيل أي موفر، تحقق من env + auth.json — لا تفترض "بلا مفتاح = لا يعمل".
- 2026-09-13: **النشر عبر GitHub Pages (وليس Vercel/Cloudflare)** — الريبو `winner-dev` عام + GitHub Pages مفعّل مسبقاً → `https://mahmoudrabeh-85.github.io/winner-dev/` حي وتأكد (v3+responsive+assets 200). الرفع: commit `30d4994`. **دروس:** (1) تحقق من `has_pages` قبل الافتراض بأننا نحتاج منصة نشر؛ (2) فحص live copy عبر HTTP + بحث نصي (responsive-fix/QR/bot link) أفضل من الاعتماد على "الدفع نجح".
- 2026-09-13: **تدوير التوكن الفعلي = بوت المراقب** — عند طلب "أرسل توكون جديداً" دوّر المستخدم بوت المراقب (@mahmoudoc26_bot) وليس بوت الموقع؛ التوكن القديم المرفوع في git history (api/telegram.js commit 73d1949) كان قد أُبطل سابقاً (401) فلا خطر حي. التوكن الجديد `8703195403:AAF3Js3T...` سُجل في supervisor/.env + البوت أُعيد تشغيله (PID 3008) + رسالة اختبار وصلت (msg_id=45). **قاعدة مضافة:** عند فحص توكن جديد — تحقق دائماً بـ getMe وقارن username بالبوت المقصود؛ لا تفترض أن "أي توكن يعمل = البوت الصحيح".
